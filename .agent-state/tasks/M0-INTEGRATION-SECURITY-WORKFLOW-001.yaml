schema_version: "1.0"
task_id: M0-INTEGRATION-SECURITY-WORKFLOW-001
title: Resolve security workflow integration conflict
task_type: REPAIR
state: READY_TO_LEASE
objective: Resolve the cherry-pick conflict for the approved security workflow commit and complete mechanical integration without changing intended security behavior.
in_scope:
  - .github/workflows/security-supply-chain.yml
out_of_scope:
  - internal/**
  - web/**
  - docs/**
  - .agent-state/**
assigned_agent: devops-release
dependencies:
  - M0-REPAIR-SECURITY-WORKFLOW-001
base_commit: 30b8ccf
acceptance_criteria:
  - The cherry-pick conflict is resolved in favor of approved security workflow behavior.
  - Cherry-pick completes on fix/m0-governance-certification.
  - Workflow YAML is valid.
verification:
  - command: python3 -c "from pathlib import Path; import yaml; yaml.safe_load(Path('.github/workflows/security-supply-chain.yml').read_text(encoding='utf-8')); print('workflow_yaml_ok')"
    expected: workflow_yaml_ok
required_reviewers:
  - security-reviewer
  - qa-reviewer
notes: Integration-conflict repair only; no new workflow scope.
