schema_version: "1.0"
task_id: M1-GOV-WORKSPACE-MODEL-001
title: Normalize Agent Kit workspace model and remove nested worktrees
task_type: REPAIR
state: HANDOFF_SUBMITTED
objective: Repair governance architecture so CANONICAL workspace mode is default, ISOLATED is optional and external-only, READ_ONLY is available for review agents, and legacy nested worktrees are removed without losing historical auditability.
in_scope:
  - .agent-state/.gitignore
  - .gitignore
  - agent-kit/**
  - examples/**
  - prompts/M0_ORCHESTRATOR_KICKOFF.md
  - tools/**
out_of_scope:
  - cmd/**
  - internal/**
  - web/**
  - migrations/**
assigned_agent: platform-devex
dependencies: []
base_commit: b98ce257cdf3efc8370e0f19e32df83b9041f40a
acceptance_criteria:
  - AuthorityLease schema supports legacy 1.0 leases and new 1.1 workspace-mode leases.
  - CANONICAL is documented as the default execution mode.
  - ISOLATED worktrees are restricted to paths outside the canonical repository.
  - READ_ONLY mode is documented for planning/review agents.
  - Nested worktrees under .agent-state/worktrees are removed and git worktree metadata is pruned.
  - Validation and test commands pass.
verification:
  - command: python3 tools/validate_agent_kit.py
    expected: "Errors: 0"
  - command: python3 tools/test_workspace_model.py
    expected: "PASS"
  - command: make lint && make test && make build
    expected: exit code 0
required_reviewers:
  - qa-reviewer
  - security-reviewer
  - documentation-reviewer
decision_refs:
  - WORKSPACE_POLICY_M1
notes: Historical M0 retrospective artifacts remain auditable and are not rewritten wholesale.

