schema_version: "1.0"
task_id: M0-REPAIR-WEB-001
title: Repair WebUI lint configuration
task_type: REPAIR
state: READY_TO_LEASE
objective: Fix the WebUI lint/tooling configuration so the M0 scaffold validates cleanly without adding product features.
in_scope:
  - web/**
out_of_scope:
  - catalog features
  - product workflows
assigned_agent: frontend
dependencies:
  - M0-CERT-WEB-001
base_commit: c0f14f25ebad3ed0e947f677198ba6cbdc8e898f
acceptance_criteria:
  - WebUI lint passes with the repository's configured command surface.
  - The M0 shell remains unchanged in scope.
verification:
  - command: cd web && npm run lint
    expected: pass
required_reviewers:
  - qa-reviewer
  - security-reviewer
notes: Tooling-only repair.
