schema_version: "1.0"
task_id: M1-AUTH-001
title: Implement authentication skeleton runtime surface
task_type: IMPLEMENTATION
state: HANDOFF_SUBMITTED
objective: Implement M1 authentication skeleton semantics for local and OIDC modes, including runtime-safe introspection endpoints and config validation.
in_scope:
  - internal/auth/**
  - internal/config/**
  - internal/runtime/**
out_of_scope:
  - internal/database/migrations/**
  - web/**
assigned_agent: auth-identity
dependencies:
  - M1-PLANNING-001
base_commit: a6d0cab77ef15ac8621e9b831f16d96a646d3c50
acceptance_criteria:
  - Auth mode is deterministically derived from config.
  - OIDC validation requires issuer, client id, and secret or secret reference when enabled.
  - Runtime exposes non-secret auth skeleton endpoints.
verification:
  - command: go test ./internal/config ./internal/runtime ./internal/auth
    expected: PASS
required_reviewers:
  - qa-reviewer
  - security-reviewer
notes: Implementation completed; handoff submitted for review.
