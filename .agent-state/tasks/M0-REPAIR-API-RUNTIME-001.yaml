schema_version: "1.0"
task_id: M0-REPAIR-API-RUNTIME-001
title: Repair API runtime health and diagnostics
task_type: REPAIR
state: READY_TO_LEASE
objective: Fix the runtime health/readiness behavior and redact diagnostic config output so M0 health and secret handling match the intended certification baseline.
in_scope:
  - internal/runtime/**
  - internal/config/**
  - internal/health/**
out_of_scope:
  - fake authentication implementation
  - new API features
assigned_agent: backend-api
dependencies:
  - M0-RECOVERY-GOV-001
  - M0-CERT-API-RUNTIME-001
base_commit: c0f14f25ebad3ed0e947f677198ba6cbdc8e898f
acceptance_criteria:
  - Readiness/startup no longer hard-code success and can reflect dependency status.
  - Diagnostic output redacts sensitive URLs and secrets.
  - Existing M0 skeleton behavior remains intentional.
verification:
  - command: go test ./internal/runtime ./internal/config ./internal/health
    expected: pass
required_reviewers:
  - qa-reviewer
  - security-reviewer
  - api-compat-reviewer
notes: Repair only; do not begin M1 auth work.
