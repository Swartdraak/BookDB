schema_version: "1.0"
task_id: M0-PLATFORM-001
title: Bootstrap cross-platform development environment
task_type: IMPLEMENTATION
state: READY_TO_LEASE
objective: Establish the BookDB developer toolchain and local service environment without implementing application features.
in_scope:
  - Dev Container
  - root task/build commands
  - development Compose
out_of_scope:
  - production HA
  - canonical data schema
  - metadata connectors
assigned_agent: platform-devex
dependencies: []
base_commit: abcdef1234567890
acceptance_criteria:
  - A clean clone can start development dependencies.
  - Go and frontend build commands are exposed through documented cross-platform tasks.
verification:
  - command: task validate
    expected: exit code 0
  - command: docker compose config
    expected: valid Compose configuration
required_reviewers:
  - qa-reviewer
  - security-reviewer
  - devops-release
decision_refs:
  - ADR-0002
notes: Do not add production application behavior.
