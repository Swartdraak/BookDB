# Skill: User Contribution Change

Map proposal state; enforce Administrator approval server-side; test Moderator cannot publish; audit decision; re-run identity conflict checks at approval; update WebUI/API docs.
